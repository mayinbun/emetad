# Bun Theme Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add a third FF14-inspired Bunny Boy theme with navy/gold/blue colors, bunny puns, 🐰 particles, and a theme-aware coffee morph (→ carrot, No → lettuce).

**Architecture:** Extend the existing `THEMES` object with a `bun` entry. Add `data-bun` attributes to all themeable elements. Make `morphCoffeeToMatcha` and `resetCoffeeToSmoothie` theme-aware by reading target emoji/label/food from `THEMES[activeTheme]`. Add 🐰 button to the theme switcher.

**Tech Stack:** Vanilla HTML/CSS/JS, existing theme system.

---

### Task 1: Add data-bun attributes to all themeable elements

**Files:**
- Modify: `index.html` (HTML body, lines ~428–480)

- [ ] **Step 1: Update page 1 elements**

Find:
```html
    <h1 class="main-heading" id="main-title">Will you date me? <span id="title-skull" data-gothic="💀" data-ironman="⚡">💀</span></h1>
    <p class="subtext" data-gothic="Even death can't stop this feeling." data-ironman="I am Iron Man.">Even death can't stop this feeling.</p>
    <div class="button-area">
      <button id="btn-yes" onclick="handleYesClick()" data-gothic="Yes 💀" data-ironman="Assemble ⚡">Yes 💀</button>
      <button id="btn-no" onclick="handleNoClick()" data-gothic="No" data-ironman="Retreat">No</button>
    </div>
```

Replace with:
```html
    <h1 class="main-heading" id="main-title">Will you date me? <span id="title-skull" data-gothic="💀" data-ironman="⚡" data-bun="🐰">💀</span></h1>
    <p class="subtext" data-gothic="Even death can't stop this feeling." data-ironman="I am Iron Man." data-bun="Hop to it.">Even death can't stop this feeling.</p>
    <div class="button-area">
      <button id="btn-yes" onclick="handleYesClick()" data-gothic="Yes 💀" data-ironman="Assemble ⚡" data-bun="Yes please! 🐰">Yes 💀</button>
      <button id="btn-no" onclick="handleNoClick()" data-gothic="No" data-ironman="Retreat" data-bun="Lettuce not.">No</button>
    </div>
```

- [ ] **Step 2: Update celebration text**

Find:
```html
    <div id="celebration-text" data-gothic="SHE SAID YES 💀🖤" data-ironman="AVENGERS ASSEMBLE ⚡🔴">SHE SAID YES 💀🖤</div>
```

Replace with:
```html
    <div id="celebration-text" data-gothic="SHE SAID YES 💀🖤" data-ironman="AVENGERS ASSEMBLE ⚡🔴" data-bun="SHE SAID YES 🐰🥕">SHE SAID YES 💀🖤</div>
```

- [ ] **Step 3: Update page 2**

Find:
```html
    <h1 class="main-heading" data-gothic="What we feeling? 💀" data-ironman="Pick your mission. ⚡">What we feeling? 💀</h1>
    <p class="subtext" data-gothic="Choose wisely." data-ironman="Choose your target.">Choose wisely.</p>
```

Replace with:
```html
    <h1 class="main-heading" data-gothic="What we feeling? 💀" data-ironman="Pick your mission. ⚡" data-bun="What's on the menu? 🐰">What we feeling? 💀</h1>
    <p class="subtext" data-gothic="Choose wisely." data-ironman="Choose your target." data-bun="Choose your carrot.">Choose wisely.</p>
```

- [ ] **Step 4: Update page 3**

Find:
```html
    <h1 class="main-heading" data-gothic="When? 💀" data-ironman="When's the mission? ⚡">When? 💀</h1>
    <p class="subtext" data-gothic="Pick your moment of doom." data-ironman="Set the coordinates.">Pick your moment of doom.</p>
```
Replace with:
```html
    <h1 class="main-heading" data-gothic="When? 💀" data-ironman="When's the mission? ⚡" data-bun="When? 🐰">When? 💀</h1>
    <p class="subtext" data-gothic="Pick your moment of doom." data-ironman="Set the coordinates." data-bun="Pick your burrow time.">Pick your moment of doom.</p>
```

Find:
```html
      <button class="confirm-btn" onclick="confirmDate()" data-gothic="It's a date 💀" data-ironman="Lock it in ⚡">It's a date 💀</button>
```
Replace with:
```html
      <button class="confirm-btn" onclick="confirmDate()" data-gothic="It's a date 💀" data-ironman="Lock it in ⚡" data-bun="It's a date 🐰">It's a date 💀</button>
```

- [ ] **Step 5: Update page 4**

Find:
```html
    <div class="confirmed-skull" data-gothic="🖤" data-ironman="⚡">🖤</div>
    <h1 class="main-heading confirmed-heading" data-gothic="It's official." data-ironman="Mission accepted.">It's official.</h1>
    <p id="confirmed-date" class="confirmed-date"></p>
    <p class="confirmed-sub" data-gothic="Don't be late. 💀" data-ironman="Don't be late. 🔴">Don't be late. 💀</p>
    <button id="copy-btn" class="confirm-btn" style="margin-top:1.5rem;width:min(260px,80vw)" onclick="copyResult()" data-gothic="Copy 🖤" data-ironman="Copy 🔴">Copy 🖤</button>
```

Replace with:
```html
    <div class="confirmed-skull" data-gothic="🖤" data-ironman="⚡" data-bun="🐰">🖤</div>
    <h1 class="main-heading confirmed-heading" data-gothic="It's official." data-ironman="Mission accepted." data-bun="Hare we go!">It's official.</h1>
    <p id="confirmed-date" class="confirmed-date"></p>
    <p class="confirmed-sub" data-gothic="Don't be late. 💀" data-ironman="Don't be late. 🔴" data-bun="Don't be late. 🐰">Don't be late. 💀</p>
    <button id="copy-btn" class="confirm-btn" style="margin-top:1.5rem;width:min(260px,80vw)" onclick="copyResult()" data-gothic="Copy 🖤" data-ironman="Copy 🔴" data-bun="Copy 🐰">Copy 🖤</button>
```

- [ ] **Step 6: Verify no visual change in browser**

```bash
open http://localhost:8765
```

Expected: site looks identical. Click through all 4 pages — no text changes.

- [ ] **Step 7: Commit**

```bash
git add index.html
git commit -m "feat: add data-bun attributes to all themeable elements"
```

---

### Task 2: Add bun entry to THEMES object and theme switcher button

**Files:**
- Modify: `index.html` (THEMES object in script block, and theme switcher HTML)

- [ ] **Step 1: Add bun to THEMES object**

Find the closing of the `ironman` entry in THEMES (around the `},` after `copySuffix`):
```js
        copySuffix: "Don't be late. 🔴",
      },
    };
```

Replace with:
```js
        copySuffix: "Don't be late. 🔴",
      },
      bun: {
        vars: {
          '--bg': '#0a0d1a',
          '--bg-secondary': '#141929',
          '--accent': '#c8a84b',
          '--accent2': '#4a90d9',
          '--text': '#f0ece0',
          '--text-dim': '#a09070',
          '--btn-yes-bg': '#c8a84b',
          '--btn-yes-hover': '#d4b85a',
          '--btn-no-bg': '#141929',
          '--btn-no-border': '#4a90d9',
          '--font-heading': "'Cinzel', serif",
        },
        particle: '🐰',
        copyPrefix: 'I said yes 🐰',
        copySuffix: "Don't be late. 🥕",
        coffeeTarget: { emoji: '🥕', food: 'Carrot', label: 'Carrot' },
        coffeeYesText: 'She wants Carrot. Hop to it. 🐰',
        coffeeNoEmoji: '🥬',
        coffeeNoFood: 'Lettuce',
        coffeeNoLabel: 'Lettuce',
        coffeeConfirmText: 'Are you sure? 🐰',
      },
    };
```

Also add default coffeeTarget/coffeeNo fields to `gothic` and `ironman` so the morph function can read from any theme:

Find `gothic: {` entry's `copySuffix` line:
```js
        copySuffix: "Don't be late. 💀",
      },
```

Replace with:
```js
        copySuffix: "Don't be late. 💀",
        coffeeTarget: { emoji: '🍵', food: 'Matcha', label: 'Matcha' },
        coffeeYesText: 'She wants Matcha. You better deliver. 💀',
        coffeeNoEmoji: '🍍🥭',
        coffeeNoFood: 'Smoothie',
        coffeeNoLabel: 'Smoothie',
        coffeeConfirmText: 'Are you sure? 💀',
      },
```

Find `ironman: {` entry's `copySuffix` line:
```js
        copySuffix: "Don't be late. 🔴",
      },
```

Replace with:
```js
        copySuffix: "Don't be late. 🔴",
        coffeeTarget: { emoji: '🍵', food: 'Matcha', label: 'Matcha' },
        coffeeYesText: 'She wants Matcha. You better deliver. ⚡',
        coffeeNoEmoji: '🍍🥭',
        coffeeNoFood: 'Smoothie',
        coffeeNoLabel: 'Smoothie',
        coffeeConfirmText: 'Are you sure? ⚡',
      },
```

- [ ] **Step 2: Add 🐰 button to theme switcher HTML**

Find:
```html
  <div class="theme-switcher">
    <button class="theme-btn theme-btn-active" data-theme="gothic" onclick="setTheme('gothic')">💀</button>
    <button class="theme-btn" data-theme="ironman" onclick="setTheme('ironman')">⚡</button>
  </div>
```

Replace with:
```html
  <div class="theme-switcher">
    <button class="theme-btn theme-btn-active" data-theme="gothic" onclick="setTheme('gothic')">💀</button>
    <button class="theme-btn" data-theme="ironman" onclick="setTheme('ironman')">⚡</button>
    <button class="theme-btn" data-theme="bun" onclick="setTheme('bun')">🐰</button>
  </div>
```

- [ ] **Step 3: Verify bun theme switches correctly in browser**

Open http://localhost:8765, click 🐰:
- Colors shift to navy/gold/blue
- All text updates (headings, buttons)
- 🐰 button gets gold glow
- Particles become 🐰 (wait 4s)

- [ ] **Step 4: Commit**

```bash
git add index.html
git commit -m "feat: add bun theme to THEMES object and switcher button"
```

---

### Task 3: Make coffee morph theme-aware

**Files:**
- Modify: `index.html` — `morphCoffeeToMatcha`, `showCoffeeConfirmBtns`, `resetCoffeeToSmoothie`

- [ ] **Step 1: Update morphCoffeeToMatcha to read from active theme**

Find and replace the entire `morphCoffeeToMatcha` function:

```js
    function morphCoffeeToMatcha(card) {
      const emojiEl = card.querySelector('.food-emoji');
      const labelEl = card.querySelector('.food-label');
      const confirmEl = document.getElementById('food-confirmation');
      const target = THEMES[activeTheme].coffeeTarget;

      emojiEl.classList.add('fade-out');

      setTimeout(() => {
        emojiEl.textContent = target.emoji;
        card.dataset.food = target.food;
        labelEl.textContent = target.label;
        emojiEl.classList.remove('fade-out');
      }, 300);

      confirmEl.textContent = THEMES[activeTheme].coffeeConfirmText;
      showCoffeeConfirmBtns(card);
    }
```

- [ ] **Step 2: Update showCoffeeConfirmBtns yes handler to use theme text**

Find and replace the entire `showCoffeeConfirmBtns` function:

```js
    function showCoffeeConfirmBtns(card) {
      removeCoffeeConfirmBtns();
      const confirmEl = document.getElementById('food-confirmation');
      const btns = document.createElement('div');
      btns.className = 'food-confirm-btns';
      btns.id = 'coffee-confirm-btns';

      const yes = document.createElement('button');
      yes.textContent = 'Yes';
      yes.onclick = () => {
        pickedFood = THEMES[activeTheme].coffeeTarget.food;
        removeCoffeeConfirmBtns();
        confirmEl.textContent = THEMES[activeTheme].coffeeYesText;
        setTimeout(showPage3, 1800);
      };

      const no = document.createElement('button');
      no.textContent = 'No';
      no.onclick = () => resetCoffeeToSmoothie(card);

      btns.appendChild(yes);
      btns.appendChild(no);
      confirmEl.insertAdjacentElement('afterend', btns);
    }
```

- [ ] **Step 3: Update resetCoffeeToSmoothie to use theme no-food**

Find and replace the entire `resetCoffeeToSmoothie` function:

```js
    function resetCoffeeToSmoothie(card) {
      const emojiEl = card.querySelector('.food-emoji');
      const labelEl = card.querySelector('.food-label');
      const confirmEl = document.getElementById('food-confirmation');
      const theme = THEMES[activeTheme];

      emojiEl.classList.add('fade-out');

      setTimeout(() => {
        emojiEl.textContent = theme.coffeeNoEmoji;
        card.dataset.food = theme.coffeeNoFood;
        labelEl.textContent = theme.coffeeNoLabel;
        emojiEl.classList.remove('fade-out');
      }, 300);

      card.classList.remove('selected');
      confirmEl.textContent = '';
      removeCoffeeConfirmBtns();
    }
```

- [ ] **Step 4: Verify coffee morph in all three themes**

Open http://localhost:8765, click through to page 2:

**Gothic:** Click Coffee → fades to 🍵 Matcha, "Are you sure? 💀", Yes → "She wants Matcha. You better deliver. 💀", No → 🍍🥭 Smoothie

**Iron Man:** Switch to ⚡, click Coffee → fades to 🍵 Matcha, "Are you sure? ⚡", Yes → "She wants Matcha. You better deliver. ⚡", No → 🍍🥭 Smoothie

**Bun:** Switch to 🐰, click Coffee → fades to 🥕 Carrot, "Are you sure? 🐰", Yes → "She wants Carrot. Hop to it. 🐰", No → 🥬 Lettuce

- [ ] **Step 5: Commit**

```bash
git add index.html
git commit -m "feat: make coffee morph theme-aware (bun: carrot/lettuce)"
```

---

### Task 4: Push to GitHub

- [ ] **Step 1: Push**

```bash
git push
```

- [ ] **Step 2: Verify on GitHub Pages** (allow ~1 min)

Open https://mayinbun.github.io/emetad/ — three theme buttons visible, 🐰 theme works end-to-end including coffee morph.